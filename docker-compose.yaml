services:
  app:
    build: .
    container_name: daris_be
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3014:${APPPORT}"
    networks:
      - mariadb_network
    volumes:
      - .env:/app/.env

networks:
  mariadb_network:
    external: true